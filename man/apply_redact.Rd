% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply_redact.R
\name{apply_redact}
\alias{apply_redact}
\title{Apply a saved redaction mapping to a new dataset}
\usage{
apply_redact(.data, mapping)
}
\arguments{
\item{.data}{A data frame or tibble containing at least the columns named
in \code{mapping}.}

\item{mapping}{A named list of tibbles as returned in \verb{$mapping} by
\code{\link[=redact]{redact()}}. Each element must have \code{original} and \code{redacted} columns.}
}
\value{
A tibble with the mapped columns replaced in-place under their
original names, analogous to the output of \code{\link[=redact]{redact()}}.
}
\description{
Re-uses the substitution tables produced by a previous \code{\link[=redact]{redact()}} call to
anonymize the same columns in a different data frame. This ensures that the
same original value always maps to the same redacted value across files.
}
\details{
Only columns present in \code{mapping} are processed; any other columns are
returned unchanged. Numeric columns are not covered by \code{mapping} (their
permutation is row-specific) — pass those columns through a fresh
\code{\link[=redact]{redact()}} call if needed.

Values in \code{.data} that were not seen during the original \code{\link[=redact]{redact()}} call
are left unchanged and a warning is issued.
}
\examples{
set.seed(1)
train <- tibble::tibble(
  sector  = c("Tech", "Finance", "Energy"),
  revenue = c(100, 200, 300)
)
result <- redact(train, col_types = list(sector = "group", revenue = "numeric"))

test <- tibble::tibble(
  sector  = c("Finance", "Tech"),
  revenue = c(150, 250)
)
apply_redact(test, result$mapping)
}

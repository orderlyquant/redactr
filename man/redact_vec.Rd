% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/redact_vec.R
\name{redact_vec}
\alias{redact_vec}
\title{Redact a single vector}
\usage{
redact_vec(x, type, bank = "auto", seed = NULL)
}
\arguments{
\item{x}{A vector to redact.}

\item{type}{Redaction type. One of:
\itemize{
\item \code{"code"}: format-preserving substitution (alpha and numeric tokens
replaced independently, separators preserved).
\item \code{"group"}: each unique value is mapped to a word from a thematic word
bank.
\item \code{"name"}: each unique value is replaced with a fake-but-realistic name.
\item \code{"numeric"}: values are randomly permuted within the vector,
preserving the distribution.
}}

\item{bank}{For \code{type = "group"} only: which word bank to draw from.
One of \code{"auto"} (pool all banks) or a specific bank name from
\code{\link[=word_bank_names]{word_bank_names()}}.}

\item{seed}{Optional integer seed for reproducibility.}
}
\value{
A list of class \code{redactr_vec_result} with elements:
\itemize{
\item \verb{$redacted}: the anonymized vector (same length and type as \code{x}).
\item \verb{$mapping}: a \code{\link[tibble:tibble]{tibble::tibble()}} with columns \code{original} and \code{redacted}
for \code{"code"}, \code{"group"}, and \code{"name"} types; \code{NULL} for \code{"numeric"}.
}
}
\description{
Anonymizes a character or numeric vector according to the specified
redaction type and returns both the redacted values and a mapping table
(where applicable).
}
\examples{
set.seed(1)
result <- redact_vec(c("ACC-001", "ACC-002", "ACC-003"), type = "code")
result$redacted
result$mapping

set.seed(2)
result <- redact_vec(c("Tech", "Finance", "Energy"), type = "group")
result$redacted

set.seed(3)
result <- redact_vec(c(10, 20, 30, 40, 50), type = "numeric")
result$redacted  # same values, different order
}

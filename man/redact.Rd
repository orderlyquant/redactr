% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/redact.R
\name{redact}
\alias{redact}
\title{Redact a tibble}
\usage{
redact(.data, col_types, seed = NULL)
}
\arguments{
\item{.data}{A data frame or tibble.}

\item{col_types}{A named list (or named character vector) specifying the
redaction type for each column to anonymize. Names must match column
names in \code{.data}. Valid types:
\itemize{
\item \code{"code"} / \code{\link[=col_code]{col_code()}}: format-preserving code substitution.
\item \code{"group"} / \code{\link[=col_group]{col_group()}}: thematic word-bank substitution.
\item \code{"name"} / \code{\link[=col_name]{col_name()}}: realistic fake-name substitution.
\item \code{"numeric"} / \code{\link[=col_numeric]{col_numeric()}}: within-column random permutation.
\item \code{"skip"} / \code{\link[=col_skip]{col_skip()}}: leave column unchanged (default for any
column not mentioned).
\item \code{\link[=col_formula]{col_formula()}}: recompute the column from an R expression evaluated
against the already-redacted data (e.g. \code{col_formula(~ alloc + selec)}).
Formula columns are processed after all other types and no mapping is
stored for them.
}}

\item{seed}{Optional integer seed passed to \code{\link[=set.seed]{set.seed()}} for
reproducibility. Applied once before any column is processed.}
}
\value{
An object of class \code{redactr_result} (a list) with elements:
\itemize{
\item \verb{$data}: a tibble with redacted columns replaced in-place under their
original names. Assign the result to a new object to preserve the
original data alongside the redacted copy.
\item \verb{$mapping}: a named list, one entry per redacted non-numeric column,
each a \code{\link[tibble:tibble]{tibble::tibble()}} with \code{original} and \code{redacted} columns.
\item \verb{$columns}: character vector of all column names that were redacted.
}
}
\description{
Anonymizes selected columns in a data frame or tibble. Each target column
is replaced in-place by a redacted version whose name carries the \verb{_rdct}
suffix. A mapping table is stored for every non-numeric column so that the
same substitutions can be applied to related files with \code{\link[=apply_redact]{apply_redact()}}.
}
\examples{
set.seed(42)
dat <- tibble::tibble(
  account_id = c("ACC-001", "ACC-002", "ACC-003"),
  sector     = c("Tech", "Finance", "Tech"),
  revenue    = c(100, 200, 150),
  manager    = c("Alice Brown", "Bob Smith", "Carol Jones")
)

result <- redact(
  dat,
  col_types = list(
    account_id = "code",
    sector     = "group",
    revenue    = "numeric",
    manager    = "name"
  )
)

result$data
result$mapping
}
